# C++ 语言期末考试 — 纠错题参考答案与解析

---

## 第 1 题

**原代码：**

```cpp
#include <iostream>
int main() {
    int& r;
    return 0;
}
```

**错误分析：** 引用必须初始化

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 2 题

**原代码：**

```cpp
#include <iostream>
int main() {
    int* p = new int[10];
    delete p;
    return 0;
}
```

**错误分析：** 数组应 delete[]

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 3 题

**原代码：**

```cpp
class Base { public: ~Base() {} };
class Derived : public Base { public: ~Derived() {} };
int main() { Base* p = new Derived(); delete p; }
```

**错误分析：** 基类析构应 virtual

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 4 题

**原代码：**

```cpp
#include <iostream>
using namespace std;
int main() { cout << "Hi" }
```

**错误分析：** 语句缺分号

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 5 题

**原代码：**

```cpp
#include <vector>
int main() { std::vector<int> v; v[0] = 1; }
```

**错误分析：** 未 push_back 就 v[0] 越界

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 6 题

**原代码：**

```cpp
void f(int x = 1, int y) {}
int main() { f(); }
```

**错误分析：** 默认参数应从右连续

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 7 题

**原代码：**

```cpp
class A { int x; };
int main() { A a; a.x = 1; }
```

**错误分析：** x 为 private 不可外访

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 8 题

**原代码：**

```cpp
#include <iostream>
int main() { std::cout << std::endl }
```

**错误分析：** 缺分号

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 9 题

**原代码：**

```cpp
int* p = nullptr;
*p = 5;
```

**错误分析：** 解引用空指针

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 10 题

**原代码：**

```cpp
#include <string>
std::string s = 'A';
```

**错误分析：** 应用双引号字符串

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 11 题

**原代码：**

```cpp
template<typename T>
T add(T a, T b) { return a+b; }
int main() { add(1, 2.0); }
```

**错误分析：** 模板参数类型不一致

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 12 题

**原代码：**

```cpp
class B : A {};
```

**错误分析：** 继承缺 public/private 关键字，class 默认 private 继承

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 13 题

**原代码：**

```cpp
#include <memory>
auto p = std::unique_ptr<int>(new int(1));
auto q = p;
```

**错误分析：** unique_ptr 不可复制

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 14 题

**原代码：**

```cpp
void f(int arr[]) { sizeof(arr); }
```

**错误分析：** 数组参数退化为指针，sizeof 非数组大小

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 15 题

**原代码：**

```cpp
#include <iostream>
namespace N { int x; }
int x;
int main() { std::cout << x; }
```

**错误分析：** 可能歧义，应 N::x

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 16 题

**原代码：**

```cpp
class C { virtual void f(); };
int main() { C c; }
```

**错误分析：** 含虚函数的类需实现或纯虚

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 17 题

**原代码：**

```cpp
#include <vector>
for (auto it = v.begin(); it != v.end(); ++it) {}
```

**错误分析：** v 未定义

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 18 题

**原代码：**

```cpp
const int MAX = 100;
MAX = 200;
```

**错误分析：** const 不可修改

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 19 题

**原代码：**

```cpp
#include <fstream>
std::ifstream f("a.txt");
f << "data";
```

**错误分析：** ifstream 不能 << 写

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 20 题

**原代码：**

```cpp
int& f() { int x=0; return x; }
```

**错误分析：** 返回局部变量引用悬垂

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 21 题

**原代码：**

```cpp
delete nullptr;
```

**错误分析：** 合法但前面若 new 需配对

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 22 题

**原代码：**

```cpp
class D: public Base { void f() override; }; // Base 无 virtual f
```

**错误分析：** override 但基类无 virtual f

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 23 题

**原代码：**

```cpp
std::cout << "n=" << n;
```

**错误分析：** n 未声明

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 24 题

**原代码：**

```cpp
#include <iostream>
main() { return 0; }
```

**错误分析：** main 缺 int 返回类型

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 25 题

**原代码：**

```cpp
enum Color { Red, Green }; int x = Red::Green;
```

**错误分析：** 普通 enum 无作用域限定

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 26 题

**原代码：**

```cpp
#include <vector>
std::vector<int>& ref = std::vector<int>{1,2};
```

**错误分析：** 绑定临时对象需 const 引用

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 27 题

**原代码：**

```cpp
class X { X(); };
```

**错误分析：** 构造函数 private 且无友元，外部无法创建

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 28 题

**原代码：**

```cpp
void swap(int a, int b) { int t=a; a=b; b=t; }
```

**错误分析：** 值传递无法交换实参

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 29 题

**原代码：**

```cpp
#include <string>
if (s = "ok") {}
```

**错误分析：** 应用 == 比较，= 是赋值

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 30 题

**原代码：**

```cpp
class A { public: A(int); }; A a;
```

**错误分析：** 无默认构造但无 int 实参

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 31 题

**原代码：**

```cpp
#include <algorithm>
std::list<int> L; std::sort(L.begin(), L.end());
```

**错误分析：** list 迭代器非随机访问，不能 sort

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 32 题

**原代码：**

```cpp
shared_ptr<int> p; p.reset(); *p = 1;
```

**错误分析：** reset 后为空，不能解引用

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 33 题

**原代码：**

```cpp
virtual void f() const;
```

**错误分析：** 若类无 const 版本虚函数，派生 override 需匹配

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 34 题

**原代码：**

```cpp
#include <iostream>
std::cin >> std::cout;
```

**错误分析：** 不能从 cout 读

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 35 题

**原代码：**

```cpp
class B { B(const B&); }; B b2 = b1;
```

**错误分析：** 若拷贝构造 private 且非 friend 则错误

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 36 题

**原代码：**

```cpp
throw "error";
```

**错误分析：** 可编译但 catch 需匹配类型，建议 std::exception

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 37 题

**原代码：**

```cpp
#include <memory>
std::auto_ptr<int> p(new int(1));
```

**错误分析：** auto_ptr 已弃用

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 38 题

**原代码：**

```cpp
namespace std { int hack; }
```

**错误分析：** 禁止在 std 加声明

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 39 题

**原代码：**

```cpp
#define CLASS class
CLASS Foo {};
```

**错误分析：** 宏污染，应用 class

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 40 题

**原代码：**

```cpp
int* p=new int(1); delete p; delete p;
```

**错误分析：** double delete 未定义行为

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 41 题

**原代码：**

```cpp
class Base {}; class Der: public Base {};
void f(Base b) {}
int main(){ Der d; f(d); }
```

**错误分析：** 值传递导致切片，应 Base& 或 Base*

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 42 题

**原代码：**

```cpp
template<class T> void g(T x) {}
g(1, 2);
```

**错误分析：** 参数个数错误

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 43 题

**原代码：**

```cpp
#include <map>
std::map<int,int> m; m[1.5]=2;
```

**错误分析：** 键类型应为 int

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 44 题

**原代码：**

```cpp
constexpr int f() { return n; } int n=1;
```

**错误分析：** constexpr 需编译期，依赖运行时 n

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 45 题

**原代码：**

```cpp
#include <vector>
std::vector<int> v(5); v.push_back() 
```

**错误分析：** push_back 需参数

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 46 题

**原代码：**

```cpp
friend void fn(); class C { friend void fn(); int x; }; void fn(){ C c; c.x=0; }
```

**错误分析：** fn 需声明 C 或定义在 C 后

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 47 题

**原代码：**

```cpp
static int s; int main(){ s=1; }
```

**错误分析：** static 成员需类外定义

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 48 题

**原代码：**

```cpp
#include <iostream>
using namespace std; using namespace std;
```

**错误分析：** 重复 using 无害但冗余

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 49 题

**原代码：**

```cpp
class A { virtual void f()=0; }; int main(){ A a; }
```

**错误分析：** 抽象类不能实例化

**改正要点：** 根据错误分析修正语法或逻辑。

---

## 第 50 题

**原代码：**

```cpp
#include <string>
std::string s="abc"; s[10];
```

**错误分析：** 越界应用 at 或检查 size

**改正要点：** 根据错误分析修正语法或逻辑。

---
