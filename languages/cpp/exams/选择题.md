# C++ 语言期末考试 — 选择题（50 道）

> 涵盖程序结构、引用、OOP、STL、智能指针等

---

## 1. C++ 源程序文件常用的扩展名是？

A. .c
B. .cpp
C. .java
D. .py

## 2. C++ 中标准输出对象位于哪个命名空间？

A. global
B. std
C. iostream
D. cout

## 3. 下列哪个是 C++ 特有的类型？

A. int
B. char
C. bool
D. float

## 4. 引用变量必须？

A. 可为空
B. 必须初始化
C. 可重新绑定
D. 可为 nullptr

## 5. nullptr 的类型是？

A. int
B. void*
C. std::nullptr_t
D. char*

## 6. delete 与 delete[] 的区别？

A. 无区别
B. delete[] 释放数组
C. delete 更快
D. 只能 delete 指针

## 7. 函数重载的依据是？

A. 返回值类型
B. 参数列表
C. 函数名长度
D. 是否 inline

## 8. 默认参数应从哪侧开始指定？

A. 左侧
B. 右侧
C. 任意
D. 中间

## 9. class 中 private 成员？

A. 类内外均可访问
B. 仅类内可访问
C. 派生类可访问
D. 全局可访问

## 10. 构造函数的名字？

A. Constructor
B. 与类名相同
C. init
D. 任意

## 11. 析构函数名前加？

A. ~
B. !
C. -
D. &

## 12. virtual 关键字的作用是？

A. 静态绑定
B. 运行时多态
C. 内联
D. 常量

## 13. override 用于？

A. 隐藏基类函数
B. 显式标记重写虚函数
C. 定义纯虚函数
D. 静态函数

## 14. 纯虚函数的写法？

A. virtual void f();
B. virtual void f() = 0;
C. void f() override;
D. abstract void f();

## 15. std::vector 的 push_back 作用？

A. 删除尾部
B. 尾部添加元素
C. 排序
D. 清空

## 16. std::string 拼接常用？

A. + 或 +=
B. strcat
C. append only
D. concat

## 17. 范围 for 循环语法？

A. for(i in v)
B. for(auto x : v)
C. foreach x in v
D. for x : v

## 18. auto 关键字作用？

A. 自动释放内存
B. 类型推导
C. 定义常量
D. 内联

## 19. iostream 中 cout 属于？

A. stderr
B. stdout 对应流
C. 文件流
D. 字符串流

## 20. #include <iostream> 与 #include <stdio.h> 在 C++ 中推荐？

A. stdio.h
B. iostream
C. 都可以无差别
D. 都不推荐

## 21. const 成员函数表示？

A. 函数是常量
B. 不修改对象状态
C. 静态函数
D. 内联函数

## 22. 静态成员变量属于？

A. 某个对象
B. 整个类共享
C. 栈
D. 仅 main

## 23. 继承方式 public 表示？

A. is-a 关系
B. has-a
C. private 继承
D. 无关系

## 24. 基类指针指向派生类对象，调用 virtual 函数？

A. 调用基类版本
B. 调用派生类版本
C. 编译错误
D. 随机

## 25. 模板函数定义？

A. template<typename T>
B. generic T
C. typename template
D. T template

## 26. std::map 存储？

A. 仅值
B. 键值对
C. 仅键
D. 指针

## 27. algorithm 中 sort 需要？

A. #include <sort>
B. #include <algorithm>
C. #include <vector> only
D. 无需头文件

## 28. unique_ptr 的特点？

A. 可复制
B. 独占所有权
C. 引用计数
D. 无析构

## 29. shared_ptr 使用引用计数？

A. 否
B. 是
C. 仅 Windows
D. 仅 Linux

## 30. RAII 的含义？

A. 资源获取即初始化
B. 随机访问
C. 运行时类型
D. 递归算法

## 31. using namespace std 在头文件中？

A. 推荐
B. 应避免
C. 必须
D. 仅 cpp 文件禁止

## 32. pragma once 作用？

A. 编译优化
B. 防止头文件重复包含
C. 定义宏
D. 命名空间

## 33. static_cast 用于？

A. 动态多态转换
B. 编译期可检查转换
C. 任意指针转换
D. 去除 const

## 34. dynamic_cast 用于？

A. 整型转换
B. 多态类型安全向下转换
C. 浮点转换
D. 数组转换

## 35. 成员初始化列表语法？

A. : member(val)
B. = member(val)
C. member(val) in body
D. init member

## 36. this 指针指向？

A. 基类
B. 当前对象
C. 全局
D. nullptr

## 37. enum class 与 enum 相比？

A. 可隐式转 int
B. 作用域更强
C. 不能 switch
D. 更快

## 38. std::vector 与 C 数组相比？

A. 大小固定
B. 大小可变
C. 不能遍历
D. 无 []

## 39. fstream 读文件用？

A. ofstream
B. ifstream
C. cout
D. ostringstream

## 40. make_unique 在哪个头文件？

A. <utility>
B. <memory>
C. <vector>
D. <unique>

## 41. 函数模板实例化发生在？

A. 链接时
B. 编译时
C. 运行时
D. 加载时

## 42. 虚析构函数的作用？

A. 加速析构
B. 多态 delete 时正确析构派生类
C. 禁止继承
D. 内联

## 43. 对象切片 (slicing) 发生在？

A. 值传递派生类给基类
B. 指针传递
C. 引用传递
D. virtual 函数

## 44. std::find 返回？

A. bool
B. 迭代器
C. int
D. void

## 45. const 引用参数常用于？

A. 修改实参
B. 避免拷贝且只读
C. 传递指针
D. 返回引用

## 46. inline 函数建议？

A. 所有函数
B. 短小的频繁调用函数
C. 递归函数
D. 虚函数

## 47. namespace 的作用是？

A. 加速编译
B. 避免命名冲突
C. 内存管理
D. 类型转换

## 48. C++ 中 main 返回值类型？

A. void
B. int
C. 任意
D. bool

## 49. std::endl 的作用？

A. 仅换行
B. 换行并刷新缓冲区
C. 清空 cout
D. 关闭流

## 50. weak_ptr 配合哪种指针？

A. unique_ptr
B. shared_ptr
C. 裸指针 only
D. auto_ptr
