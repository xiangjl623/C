# C 语言期末考试 — 纠错题参考答案与解析

---
## 第 1 题

**原代码：**

```c
#include <stdio.h>
main() {
    printf("Hello");
}
```

**改正说明：** main缺少返回类型int；应写 int main(void)

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 2 题

**原代码：**

```c
int main() {
    int a = 5
    return 0;
}
```

**改正说明：** 第2行末尾缺少分号

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 3 题

**原代码：**

```c
int main() {
    if (a > 0)
        printf("yes");
        printf("always");
    return 0;
}
```

**改正说明：** 缺少{}，第二条printf不在if内

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 4 题

**原代码：**

```c
int main() {
    int i;
    for (i = 0; i < 10; i--)
        printf("%d", i);
    return 0;
}
```

**改正说明：** 死循环：应为 i++

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 5 题

**原代码：**

```c
int main() {
    int arr[5] = {1,2,3,4,5,6};
    return 0;
}
```

**改正说明：** 初始化元素个数超过数组大小

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 6 题

**原代码：**

```c
int main() {
    char c = "A";
    return 0;
}
```

**改正说明：** 字符不能用双引号字符串赋值，应 'A'

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 7 题

**原代码：**

```c
int main() {
    float x = 3.14;
    printf("%d", x);
    return 0;
}
```

**改正说明：** 格式符应为 %f 而非 %d

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 8 题

**原代码：**

```c
int main() {
    int *p;
    *p = 10;
    return 0;
}
```

**改正说明：** p未初始化，野指针解引用

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 9 题

**原代码：**

```c
int main() {
    int a[3];
    a[3] = 0;
    return 0;
}
```

**改正说明：** 数组越界，合法下标0-2

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 10 题

**原代码：**

```c
void swap(int a, int b) {
    int t = a; a = b; b = t;
}
```

**改正说明：** 值传递无法交换，应传指针

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 11 题

**原代码：**

```c
int main() {
    char s[5] = "hello";
    return 0;
}
```

**改正说明：** 字符串含\0需6字节，数组太小

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 12 题

**原代码：**

```c
int main() {
    if (x = 5) printf("ok");
    return 0;
}
```

**改正说明：** 条件应 == 而非 =

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 13 题

**原代码：**

```c
int factorial(int n) {
    return n * factorial(n-1);
}
```

**改正说明：** 缺少递归终止条件 n<=1

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 14 题

**原代码：**

```c
int main() {
    switch(2.5) { case 1: break; }
    return 0;
}
```

**改正说明：** switch表达式不能是浮点

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 15 题

**原代码：**

```c
#define SQ(x) x*x
int main() {
    printf("%d", SQ(1+2));
    return 0;
}
```

**改正说明：** 宏应 #define SQ(x) ((x)*(x))

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 16 题

**原代码：**

```c
int main() {
    int a = 10;
    int b = 10 / 0;
    return 0;
}
```

**改正说明：** 除零未定义（逻辑/运行时错误）

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 17 题

**原代码：**

```c
int main() {
    char *s = "test";
    s[0] = 'T';
    return 0;
}
```

**改正说明：** 字符串字面量不可修改

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 18 题

**原代码：**

```c
int main() {
    printf("%s", 'a');
    return 0;
}
```

**改正说明：** %s需要char*，'a'是int字符常量

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 19 题

**原代码：**

```c
int main() {
    scanf("%d", x);
    return 0;
}
```

**改正说明：** scanf需要变量地址 &x

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 20 题

**原代码：**

```c
int main() {
    double d;
    scanf("%f", &d);
    return 0;
}
```

**改正说明：** double应用 %lf

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 21 题

**原代码：**

```c
struct Point { int x,y; };
int main() {
    Point p;
    p->x = 1;
    return 0;
}
```

**改正说明：** p是变量不是指针，应用 p.x

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 22 题

**原代码：**

```c
int main() {
    FILE *fp = fopen("a.txt", "r");
    fclose(fp);
    fgetc(fp);
    return 0;
}
```

**改正说明：** 关闭后不能再使用fp

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 23 题

**原代码：**

```c
int main() {
    int *p = malloc(10);
    return 0;
}
```

**改正说明：** malloc后未free，内存泄漏

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 24 题

**原代码：**

```c
int main() {
    free(NULL);
    free(p);
    free(p);
    return 0;
}
```

**改正说明：** 重复free同一指针

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 25 题

**原代码：**

```c
int max(int a,int b,int a) { return a>b?a:b; }
```

**改正说明：** 形参重复命名a

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 26 题

**原代码：**

```c
int main() {
    unsigned int i;
    for(i=10; i>=0; i--)
        printf("%u", i);
    return 0;
}
```

**改正说明：** unsigned i>=0永真，死循环

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 27 题

**原代码：**

```c
int main() {
    char s[10];
    gets(s);
    return 0;
}
```

**改正说明：** gets已废弃，用fgets

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 28 题

**原代码：**

```c
int main() {
    strcpy(s, "long string here");
    return 0;
}
```

**改正说明：** s未定义或缓冲区不足

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 29 题

**原代码：**

```c
int main() {
    int n;
    while(n > 0) { n--; }
    return 0;
}
```

**改正说明：** n未初始化

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 30 题

**原代码：**

```c
int add(int a,b) { return a+b; }
```

**改正说明：** 缺少参数类型 int b

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 31 题

**原代码：**

```c
int main() {
    return 0
}
```

**改正说明：** return语句后缺少分号

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 32 题

**原代码：**

```c
int main(void) {
    2 = a;
    return 0;
}
```

**改正说明：** 左值不能是常量2

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 33 题

**原代码：**

```c
int main() {
    enum {A=1,B,C};
    printf("%d", C);
    return 0;
}
```

**改正说明：** 本题可运行(C=2)，若考查enum需明确；改为未定义变量更易错

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 34 题

**原代码：**

```c
int main() {
    const int x = 5;
    x = 10;
    return 0;
}
```

**改正说明：** 不能修改const变量

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 35 题

**原代码：**

```c
int main() {
    int arr[];
    return 0;
}
```

**改正说明：** 数组大小未知必须指定或初始化

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 36 题

**原代码：**

```c
int main() {
    printf("%d\n", sizeof(a));
    return 0;
}
```

**改正说明：** 变量a未声明

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 37 题

**原代码：**

```c
int f() { return; }
```

**改正说明：** 非void函数return需返回值

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 38 题

**原代码：**

```c
void g() { return 5; }
```

**改正说明：** void函数不能return值

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 39 题

**原代码：**

```c
int main() {
    int x = 5;
    if(x); printf("hi");
    return 0;
}
```

**改正说明：** if后分号使printf不在if内

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 40 题

**原代码：**

```c
int main() {
    char ch = '\0';
    if(ch == '\0') printf("empty");
    else if(ch == NULL) printf("null");
    return 0;
}
```

**改正说明：** char与NULL指针比较不当

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 41 题

**原代码：**

```c
int *f() { int x=5; return &x; }
```

**改正说明：** 返回局部变量地址，悬空指针

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 42 题

**原代码：**

```c
int main() {
    int a[2][3] = {1,2,3,4,5};
    return 0;
}
```

**改正说明：** 初始化元素不足或格式错误

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 43 题

**原代码：**

```c
int main() {
    printf("%c", "A");
    return 0;
}
```

**改正说明：** %c要char，"A"是地址

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 44 题

**原代码：**

```c
int main() {
    int i=0;
    while(i<10); {
        printf("%d", i);
        i++;
    }
    return 0;
}
```

**改正说明：** while后分号导致空循环

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 45 题

**原代码：**

```c
int main() {
    #include <stdio.h>
    return 0;
}
```

**改正说明：** #include不能在函数体内

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 46 题

**原代码：**

```c
int main() {
    int x = ++x;
    return 0;
}
```

**改正说明：** 自增自身未定义行为

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 47 题

**原代码：**

```c
int main() {
    printf("%d", i++ + ++i);
    return 0;
}
```

**改正说明：** 同一表达式多次修改i，未定义

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 48 题

**原代码：**

```c
int main() {
    char s[] = "abc";
    if(s == "abc") printf("eq");
    return 0;
}
```

**改正说明：** 数组名与字符串字面量比较地址非内容

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 49 题

**原代码：**

```c
int compare(char a[], char b[]) {
    return a == b;
}
```

**改正说明：** 应使用strcmp比较字符串

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 50 题

**原代码：**

```c
int main() {
    int x = (int)3.14;
    printf("%f", x);
    return 0;
}
```

**改正说明：** x是int，应用%d

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 51 题

**原代码：**

```c
int main() {
    static int count;
    count++;
    return count;
}
```

**改正说明：** 本题正确；改为：auto static int x; 非法

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 52 题

**原代码：**

```c
int main() {
    auto static int x = 0;
    return 0;
}
```

**改正说明：** storage class顺序/重复错误

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 53 题

**原代码：**

```c
int main() {
    float f = 3/2;
    printf("%.1f", f);
    return 0;
}
```

**改正说明：** 3/2为1非1.5，应3.0/2

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 54 题

**原代码：**

```c
int main() {
    char c = 128;
    if(c == 128) printf("eq");
    return 0;
}
```

**改正说明：** char可能存不下128，比较异常

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 55 题

**原代码：**

```c
int main() {
    int len = strlen("hi");
    return 0;
}
```

**改正说明：** 缺少 #include <string.h>

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 56 题

**原代码：**

```c
int main() {
    int *p = NULL;
    printf("%d", *p);
    return 0;
}
```

**改正说明：** 解引用空指针

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 57 题

**原代码：**

```c
int main() {
    int a, b;
    scanf("%d %d", &a, &a);
    return 0;
}
```

**改正说明：** 同一地址读两次逻辑错误

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 58 题

**原代码：**

```c
int binarySearch(int a[], int n, int key) {
    int l=0, h=n;
    while(l<=h) { ... }
}
```

**改正说明：** h应初始化为n-1

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 59 题

**原代码：**

```c
int sum(int n) {
    int s=0, i;
    for(i=1; i<=n; i++);
        s += i;
    return s;
}
```

**改正说明：** for后分号使s+=不在循环内

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 60 题

**原代码：**

```c
int isPrime(int n) {
    if(n<2) return 0;
    for(int i=2; i*i<=n; i++)
        if(n%i==0) return 0;
    return 1;
}
```

**改正说明：** C89不能在for内声明i，需提前声明

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 61 题

**原代码：**

```c
int main() {
    typedef struct { int x; } Node;
    Node n;
    n.x = 1;
    struct Node n2;
    return 0;
}
```

**改正说明：** typedef后应直接用Node非struct Node

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 62 题

**原代码：**

```c
int main() {
    union U { int i; float f; } u;
    u.i = 5;
    printf("%f", u.f);
    return 0;
}
```

**改正说明：** union读未写入的成员，值不确定（教学题）

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 63 题

**原代码：**

```c
int main() {
    int arr[5] = {0};
    memset(arr, 1, 5);
    return 0;
}
```

**改正说明：** memset按字节设1非全1；且需string.h

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 64 题

**原代码：**

```c
int main() {
    char fmt[] = "%d";
    printf(fmt, 3.14);
    return 0;
}
```

**改正说明：** 格式与参数类型不匹配

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 65 题

**原代码：**

```c
int main() {
    int x = 077;
    printf("%d", x);
    return 0;
}
```

**改正说明：** 077是八进制63，非77（逻辑理解题）

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 66 题

**原代码：**

```c
int main() {
    printf("%d", sizeof('a'));
    return 0;
}
```

**改正说明：** 字符常量在C中sizeof通常为int大小

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 67 题

**原代码：**

```c
int main() {
    int i = 0;
    do; while(0);
    return 0;
}
```

**改正说明：** do后缺少循环体语句

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 68 题

**原代码：**

```c
int main() {
    case 1: printf("x"); break;
    return 0;
}
```

**改正说明：** case必须在switch内

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 69 题

**原代码：**

```c
int main() {
    default: break;
    return 0;
}
```

**改正说明：** default必须在switch内

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 70 题

**原代码：**

```c
int main() {
    int x[10], *p=x;
    printf("%d", x-p);
    return 0;
}
```

**改正说明：** x-p为0正确；改为 p-x类型或越界指针相减

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 71 题

**原代码：**

```c
void print(int arr[10]) {
    printf("%d", sizeof(arr));
    return 0;
}
```

**改正说明：** void函数不能return 0；sizeof(arr)是指针大小

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 72 题

**原代码：**

```c
int main() {
    register int r;
    &r;
    return 0;
}
```

**改正说明：** 不能对register变量取地址

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 73 题

**原代码：**

```c
int main() {
    extern int x;
    int x = 5;
    return 0;
}
```

**改正说明：** extern声明与定义冲突在同一作用域

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 74 题

**原代码：**

```c
int main() {
    #define N 10
    int a[N+1];
    return 0;
}
```

**改正说明：** 宏在运行时数组变长需C99 VLA；旧标准可能问题

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 75 题

**原代码：**

```c
int main() {
    long long x = 1 << 40;
    return 0;
}
```

**改正说明：** 1是int，移位可能溢出，应 1LL<<40

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 76 题

**原代码：**

```c
int main() {
    printf("%d", true);
    return 0;
}
```

**改正说明：** C中true需stdbool.h或自行定义

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 77 题

**原代码：**

```c
int main() {
    bool flag = 1;
    return 0;
}
```

**改正说明：** bool需 #include <stdbool.h>

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 78 题

**原代码：**

```c
int main() {
    int a=1, b=2, c=3;
    if(a<b<c) printf("yes");
    return 0;
}
```

**改正说明：** a<b<c解析为(a<b)<c即0<c，非链式比较

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 79 题

**原代码：**

```c
int main() {
    char *p = malloc(0);
    strcpy(p, "hi");
    return 0;
}
```

**改正说明：** malloc(0)行为实现定义，空间不足

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 80 题

**原代码：**

```c
int main() {
    int n=-1;
    unsigned u=1;
    if(n < u) printf("yes");
    return 0;
}
```

**改正说明：** 有符号与无符号比较，n转为大正数

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 81 题

**原代码：**

```c
int main() {
    char s[10]="";
    strcat(s, "hello world");
    return 0;
}
```

**改正说明：** 缓冲区溢出

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 82 题

**原代码：**

```c
int main() {
    int i, s=0;
    for(i=0;i<10;i++);
        s+=i;
    return 0;
}
```

**改正说明：** for后分号，s只加一次i=10

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 83 题

**原代码：**

```c
int main() {
    printf("%s", 123);
    return 0;
}
```

**改正说明：** %s需要char*，123是int

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 84 题

**原代码：**

```c
int main() {
    int x = 5;
    int y = x++ + x++;
    return 0;
}
```

**改正说明：** 同一表达式多次修改x，未定义

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 85 题

**原代码：**

```c
int main() {
    char c;
    scanf("%c", &c);
    scanf("%c", &c);
    return 0;
}
```

**改正说明：** 连续读字符需消空白或读\n

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 86 题

**原代码：**

```c
int main() {
    int arr[5];
    arr = {1,2,3,4,5};
    return 0;
}
```

**改正说明：** 数组不能整体赋值

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 87 题

**原代码：**

```c
int main() {
    int (*fp)() = main;
    fp(1,2,3);
    return 0;
}
```

**改正说明：** main参数不匹配或fp类型错误

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 88 题

**原代码：**

```c
struct S { char c; int i; };
int main() {
    printf("%d", sizeof(struct S));
    return 0;
}
```

**改正说明：** 考查对齐，非错误；若认为padding则说明

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 89 题

**原代码：**

```c
int main() {
    int i=0;
    while(i++ < 5)
        continue;
        printf("%d", i);
    return 0;
}
```

**改正说明：** continue后printf不在循环内，缺{}

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 90 题

**原代码：**

```c
int main() {
    #ifdef DEBUG
    printf("debug");
    return 0;
}
```

**改正说明：** 缺少 #endif

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 91 题

**原代码：**

```c
int main() {
    int a[2] = {1,2};
    int *p = a;
    printf("%d", p[2]);
    return 0;
}
```

**改正说明：** p[2]越界

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 92 题

**原代码：**

```c
int main() {
    double d = 0.1 + 0.2;
    if(d == 0.3) printf("eq");
    return 0;
}
```

**改正说明：** 浮点精度，应比较差值epsilon

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 93 题

**原代码：**

```c
int main() {
    char s[5]="12345";
    return 0;
}
```

**改正说明：** 无空间存'\0'

**考点解析：** 本题属于【内存错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 94 题

**原代码：**

```c
int main() {
    int x = -1;
    unsigned y = 1;
    if(x > y) printf("yes");
    return 0;
}
```

**改正说明：** x转为unsigned后很大，逻辑易错

**考点解析：** 本题属于【类型错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 95 题

**原代码：**

```c
int main() {
    printf("Hello World")
    return 0;
}
```

**改正说明：** printf后缺少分号

**考点解析：** 本题属于【控制流错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 96 题

**原代码：**

```c
int main() {
    int 2var = 0;
    return 0;
}
```

**改正说明：** 标识符不能以数字开头

**考点解析：** 本题属于【语法错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 97 题

**原代码：**

```c
int main() {
    return main();
}
```

**改正说明：** 无限递归栈溢出

**考点解析：** 本题属于【逻辑错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 98 题

**原代码：**

```c
int main() {
    char ch = 'A';
    switch(ch) {
        case 'A': printf("A");
        case 'B': printf("B");
    }
    return 0;
}
```

**改正说明：** case A后缺少break，贯穿

**考点解析：** 本题属于【指针错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 99 题

**原代码：**

```c
int main() {
    int i;
    for(i=0; i<10; i++) {
        if(i==5)
            break;
        else
            continue;
        printf("%d", i);
    }
    return 0;
}
```

**改正说明：** continue后printf不可达

**考点解析：** 本题属于【数组字符串错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

## 第 100 题

**原代码：**

```c
int main() {
    int n = 5;
    int arr[n];
    return 0;
}
```

**改正说明：** VLA需C99；部分旧编译器不支持

**考点解析：** 本题属于【预处理错误】类常见问题，需结合编译器报错信息与语义分析定位。

**思路整理：**
1. 先区分语法错误（编译不过）与逻辑错误（结果不对）。
2. 检查括号、分号、类型、边界、指针初始化。
3. 对照正确写法改写并 mentally 走查执行流程。

---

