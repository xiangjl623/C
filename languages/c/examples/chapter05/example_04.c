#include <stdio.h>

int main() {
    int a = 10, b = 5;
    
    printf("a > b: %d\n", a > b);   // 1锛堢湡锛?    printf("a < b: %d\n", a < b);   // 0锛堝亣锛?    printf("a >= b: %d\n", a >= b); // 1锛堢湡锛?    printf("a <= b: %d\n", a <= b); // 0锛堝亣锛?    printf("a == b: %d\n", a == b); // 0锛堝亣锛?    printf("a != b: %d\n", a != b); // 1锛堢湡锛?    
    return 0;
}
